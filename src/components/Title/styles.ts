import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    title: {
        flexDirection: 'row',
        gap: 10,
        alignItems: 'center',
    },
    textTitle: {
        color: 'white',
        fontSize: 18,
        fontWeight: '600',
    },
});

export default styles;